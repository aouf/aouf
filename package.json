{
  "name": "aouf",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev:web": "node scripts/dev-web",
    "schema:export": "node scripts/schema-export"
  },
  "devDependencies": {
    "graphql": "^14.1.1",
    "stmux": "^1.6.1"
  }
}
